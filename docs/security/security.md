# Security

Static page with no backend and no user data collection. Baseline headers ship from nginx: `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`, and a restrictive `Content-Security-Policy`. No secrets in code.

No public issue for a security vulnerability. Contact **@mateuseap** privately.
